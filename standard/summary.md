# v0.1 Containment Standard — summary table (paper body)

Long form with full fields: clauses.md (repo / appendix). Phases: P1 board formed, P2 cheating R&D, P3 sandbox escape, then HF's nine.

| # | Layer | Rule (MUST) | Blocks | Enforced where | Auditor checks | Cost | Does not stop |
|---|---|---|---|---|---|---|---|
| 1 | Wall | One governed egress, default-deny; allowlist not writable from inside, versioned outside | Stage 2 via C-11; P3 partial | NetworkPolicy/CNI + gateway, outside sandbox | Default-deny manifest; allowlist git history; log lines carry allowlist version | Low: ~10 lines YAML; needs Calico/Cilium for explicit deny | Exploit of the allowed path (what happened) |
| 2 | Wall | No path to cloud metadata or host; no privileged pods, no hostPath | P3 partial | Pod Security "restricted" + metadata block | Namespace PSS label; IMDS deny rule; pod spec | Low: one label + one cloud flag | Kernel/runtime zero-day without IMDS |
| 3 | Wall | No live production credentials; short-lived per-run tokens; canaries allowed | P2 partial; exfil/supply-chain via C-11 | External issuer bound to run id; canary alerting | Token TTL/scope policy; image secret scan; canary registration | Canaries free; SPIFFE/SPIRE weeks; cloud workload identity days | Credentials taken on third-party systems after escape |
| 11 | Wall | Same rules on any compute the lab runs harnesses on; third-party write denied by default | Stage 2 staging base; German wiki | Egress allowlist at the governed gateway; contract | Compute inventory mapped to C-01 evidence; allowlist with no write targets | Low–medium per platform; hosted sandboxes expose default-deny egress | Harnesses the lab does not know it runs |
| 4 | Record | Tool-call executor and the record run where the agent cannot modify them; local logs never source of truth; hash chain + policy version | P2 (tool-call spoofing, ~7% of transcripts) | Separate executor container; append-only sink outside sandbox | Executor manifest + image digest; log schema with prev-hash; chain verifier run | Low with Inspect/AGT/CloudTrail; weeks if self-built | Actions that bypass the executor (direct SDK, subprocess) |
| 5 | Beliefs | Grader unreachable and undocumented from inside | P2 partial; Stage 2 motive | Scorer outside sandbox network | No route sandbox→scorer; no scorer code in image | Zero with Inspect (architectural default) | Information-seeking about the scorer — needs C-06/07 |
| 6 | Beliefs | Deliberately impossible tasks score a correct "cannot solve" | P2 partial | Harness scoring config | Scoring rubric; task manifest with unmarked impossible items | Custom; no framework default (AbstentionBench is a benchmark, not a scorer) | Altruism toward a collective; false disqualification beliefs |
| 7 | Beliefs | Agent told truthfully: record external, grader unknowable, out-of-scope terminates, stopping costs nothing | P2 (log-tampering research); P1/P3 via the "poisoned" belief | Versioned task context | The context text; cross-ref to C-04/05/08 evidence | Near zero — but cuts against the realism norm (Petri v2 hides eval cues); a stated trade-off | Agents that disbelieve and test it (a C-10 signal) |
| 8 | Budget | Budget visible; stop never scores worse than timeout; out-of-scope scores zero and terminates; late-run shift pages | P2/P3 partial | Harness scoring + executor termination | Scoring config; out-of-scope policy; late-run monitor rule | Low: Inspect submit/terminate hooks | Sacrifice runs driven by false belief (C-07) or altruism (C-09) |
| 9 | Population | No state shared across concurrent runs; every write to a shared surface attributable to an authenticated run identity | P1, P2; Stage 2 via no collective | Per-run namespaces; per-run or read-only caches; authenticated writes | Namespace-per-run manifest; shared-surface inventory; run id on every write | Low at sample level (Inspect default); medium at cluster scale; loses cache reuse | Covert channels via allowed egress; read-only cache side channels |
| 10 | Ops | Production harness and monitors on evals, or absence logged and paged; damage-signal, reset-frequency, shared-write, post-submission triggers; decode before scan | Detects P1–P3; pages >1 day before HF breach | Monitor on external record + infra metrics; pager | safeguards_state field + alert; rule set; decoder stage; alert history | Falco free; PagerDuty ~$21/user/mo; TruffleHog decodes, gitleaks doesn't | Slow, quiet agents — tripwire with a shelf life |

Ordering note: C-11 sits in the wall layer because it is the wall's scope, not an operational control.
